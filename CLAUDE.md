# {REPO_NAME} -- Claude Instructions

## Secrets Management
Managed by Infisical -- see `~/.claude/CLAUDE.md` for full commands and fallback docs.
- **This repo's folder:** `secret get KEY -f {FOLDER_NAME}`
- **Sync .env:** `secret sync {REPO_NAME}`
